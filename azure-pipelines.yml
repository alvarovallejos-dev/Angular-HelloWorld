# Node.js with Angular
# Build a Node.js project that uses Angular.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '20.x'
  displayName: 'Install Node.js'

- script: |
    npm install -g @angular/cli
    npm install
    ng build --prod
  displayName: 'npm install and build'
- task: DockerCompose@1
  inputs:
    containerregistrytype: 'Azure Container Registry'
    azureSubscription: 'Suscripción de Azure 1(c2aa387d-2bbd-4cab-a35e-223b28050ea8)'
    azureContainerRegistry: '{"loginServer":"acrdemduoc.azurecr.io", "id" : "/subscriptions/c2aa387d-2bbd-4cab-a35e-223b28050ea8/resourceGroups/rg-grupo-x/providers/Microsoft.ContainerRegistry/registries/acrdemduoc"}'
    dockerComposeFile: '**/docker-compose.yml'
    action: 'Run a Docker Compose command'